<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.chaquo.python.android.PyApplication"
        >
        <activity android:name="ca.mcmaster.testsuitecommon.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ca.mcmaster.testsuitecommon.DeviceScanActivity" />
        <activity android:name=".MeasurementActivity" />
        <activity android:name=".Prefs" />
        <activity android:name=".DriveExport" />
        <activity android:name="ca.mcmaster.potentiostat.ExpSelectActivity" />
        <activity android:name="ca.mcmaster.potentiostat.ExpEditActivity" />

        <service
            android:name="ca.mcmaster.testsuitecommon.BluetoothLeService"
            android:enabled="true" />

        <activity android:name="ca.mcmaster.potentiostat.ResultsActivity" />
    </application>

</manifest>